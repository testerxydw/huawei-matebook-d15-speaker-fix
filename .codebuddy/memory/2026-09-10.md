# 2026-09-10

## 双方没声排查：SOF 固件 IPC 超时复发（与麦路由修复无关）
- 现象：系统扬声器与耳机均无声。
- 排查结论：
  - 声卡 card0 sofessx8336 存在；PipeWire 运行；内核仍为 6.18.48-amd64-desktop-rolling（修复内核）；耳机播放控件 [on]、音量 100%；`Differential Mux=1`(lin2-rin2)、`Digital Mic Mux=0`(dmic disable) 均正确（麦路由修复生效）。
  - **主因：SOF 固件 IPC 超时复发**。`dmesg` 满屏 `sof-audio-pci-intel-tgl ... IPC timeout` / `pcm0 (ES8336) STREAM_PCM_PARAMS ipc failed` / `ASoC error (-110)`。扬声器+耳机共用 ES8336 pcm0，IPC 超时导致 hw_params 失败 → 两边都无法播放。
  - 时间线：开机 2026-09-09 17:28；**首次 IPC timeout 2026-09-10 10:15:54**，距昨日 18:00 部署约 16 小时，期间无 suspend/resume → **与麦路由改动无关，是原始固件级问题独立复发**。累计 443 次，10:57 仍在发生。
  - 次因（仅扬声器）：功放 0x01=0x00（扬声器静音）、`Speaker` ALSA 开关 [off]——可能 jack 被判为插入或残留；不影响耳机（耳机控件为 on）。

## 模块层深挖（用户质疑“重启有用？重载丢设备”后）
- 当前加载 `snd_soc_sof_es8336` srcversion `C02936E8` = README 所述**原版/stock**，部署正确、非问题根源。
- `kernel/backup/` 里的自定义 `.ko`（snd-soc-sof_es8336 / snd-soc-es8316）vermagic 是 **6.18.38**，本机现跑 **6.18.48** → **不兼容、加载不了**；且补丁版早已回退（会导致 codec 掉电失声 + 驱动重载 -EBUSY）。故“部署旧自定义模块”是死路。
- 已装内核一堆：6.12.x 与 6.18.19~6.18.48（当前 6.18.48 最新）；6.18.38 也在列（自定义 .ko 正是为它编的，但该模块已回退不用）。
- 运行时电源：`/sys/bus/pci/devices/0000:00:1f.3/power/control=on`、`runtime_status=active` → 不是 runtime PM 挂起导致唤醒失败。
- 结论：**没声 = SOF DSP 固件(sof-adl.ri.zst)长时间运行后自发 wedged（IPC 超时），机器驱动层面没问题**。重启能恢复但约 17h 后复发；模块重载按用户经验会丢设备、不可用。
- 真正修复须在**内核/固件层**：① apt 看有无更新内核/linux-firmware 带更稳 SOF；② 为 6.18.48 重新编译打过补丁的 snd-soc-sof_es8336（修 remove() 的 gpiod 泄漏，即“重载 -EBUSY/丢设备”根因，但需构建环境且补丁版曾引 codec 掉电，需谨慎）；③ 权宜之计：做“检测 dmesg IPC timeout → 安全重启”的看门狗。

## 待用户决策
- 立即恢复声音：只能重启（临时，约 17h 后复发）。
- 根治方向待选：查 apt 更新内核/固件 / 重新编译补丁驱动 / 加看门狗自动恢复。

## 只读深挖 SOF wedge 根因（sudo 探测，未改任何配置）
- **确认加载配置**（来自 `/sys/kernel/debug/sof/fw_profile/`）：fw=`sof-adl.ri`、ipc_type=`0`(IPC3)、tplg=`sof-adl-es8336-dmic2ch-ssp0.tplg`。
- **固件版本**：`2:2:0-57864`；**Firmware ABI 3:22:1 / Kernel ABI 3:23:1 → ABI 小版本不匹配（22 vs 23），固件偏旧**（固件来自 `linux-firmware` 包 `20250310...deepin17`）。
- **wedge 性质**：`fw_state`/`exception`/`stream`/`debug` 全空，`etrace` 是二进制需 `sof-logger`（本机未装）→ 固件是 **IPC 挂死(hang) 而非 panic**（无异常回溯）。debugfs 在 wedged 态完全失能，无法就地恢复，只能重载固件。
- **触发线索强烈指向 DMIC**：首次 IPC timeout 在 09:05 的 `pcm1 (DMIC) dir 1`（捕获），10:15 后才蔓延到 `pcm0 (ES8336) dir 0`（播放）。当前 tplg 带 `dmic2ch`，本机模拟麦走 es8336 的 Differential Mux（我们的修复），**根本不需要 DMIC**。
- **候选防触发 topology（无 dmic）**：`sof-adl-es8336-ssp0/ssp1/ssp2.tplg.zst` 均存在。
- `snd_sof` 真实可用参数：`ipc_type`(0/1)、`ipc4_ignore_cpc`(关固件时钟缩放)、`tplg_filename`、`fw_filename`、`disable_function_topology`、`sof_debug`。`snd_intel_dspcfg.dsp_driver` 可切 1=legacy(会丢声)/4=AVS。
- 本机无 `/sys/bus/pci/devices/0000:00:1f.3/reset`，恢复只能走 unbind/bind 或重启；debugfs 有可写 `skip_imr_boot`（跳过固件 IMR，配合重启，若 IMR 损坏可用）。

### 结论性方向（按置信度）
- **防（最推荐，可逆、低风险）**：强制非 dmic topology `snd_sof.tplg_filename=sof-adl-es8336-ssp1.tplg`，直接去掉疑似触发源的 DMIC 管线；模拟麦路由修复不受影响。
- **防（根治期望高）**：更新 `linux-firmware` 拿 ABI 3:23:1 匹配的 sof-adl.ri；或更新 rolling 内核。
- **防（换通路）**：`snd_sof.ipc_type=1 snd_sof.ipc4_ignore_cpc=1`（IPC4 + 关时钟缩放）+ 配套 IPC4 tplg。
- **恢复**：重启（临时）/ PCI unbind+bind（折中，需实测）/ 看门狗自动恢复 / `skip_imr_boot`+重启。
- 用户要求：仅探索不修改。尚未落地任何改动。

## 上游 git 历史核查（sof 固件/IPC 是否修过，2026-09-10）
- sof-bin 版本号已改日期制：最新 **v2025.12.2**（旧 v2.x 止于 v2.2.6）。发行版 deepin 自带 `linux-firmware 20250310...deepin17` ≈ 2025-03 内容，远旧于 v2025.12。
- thesofproject/sof 中 es8336 相关 PR **全部是拓扑/配置修复，没有一条"IPC 超时修复"**：
  - #5204 加 ES8336 全排列拓扑(SSP0/2, DMIC 0/2/4ch)；#5368 修 cml-es8336 错误 19.2MHz→24MHz MCLK；#5460 glk-es8336 改 mclk ID0(原 ID1 错)；#6018/#6078 补 APL/GLK/全平台 ES8336 的 DMIC/SSP1 拓扑；#6878/#7536 明确指出 **ADL-es8336 拓扑"botched with missing topologies and errors in topology names"**；#8993(2024) 4ch DMIC 拓扑；#10048(2025) Everest+HDMI-in。
- 社区 `sof-essx8336-debian-fix` 明确：'The correct SOF topologies for the device don't seem to be part of sof-bin' → 官方 sof-bin 不含设备正确拓扑，需 replacement topologies。
- archlinuxcn 同款华为(ADL-P+ES8336+Device 3e5d)帖子：间歇性 SOF IPC 超时 -110、DMIC hw_params 失败、pipewire 卡死，根因指向拓扑/DMIC 不匹配。
- **结论**：IPC 超时在新版固件里**没有专门修复提交**，它是 es8336 拓扑/时钟/DMIC 配置缺陷（官方 ADL 拓扑曾称 botched）；新版 sof-bin 拓扑更全但仍为通用排列组合、不含华为机型 quirk，单纯升级固件未必根治。对症仍是拓扑层绕开 DMIC（方向①：强制非 dmic 拓扑）。

## UCM 排查：耳机能否脱离手写 amixer（2026-09-10，仅只读）
- deepin **已自带** es8336 UCM：`/usr/share/alsa/ucm2/Intel/sof-essx8336/{HiFi.conf,sof-essx8336.conf,Hdmi.conf}`，声卡名 `sofessx8336`(card0) 对应。
- 运行时佐证 UCM 已生效：`Digital Mic Mux=0`(dmic disable)、`DAC Source Mux=0`、HP mixer DAC 开关均 on，且与 UCM BootSequence 一致。
- **唯一缺口=耳麦麦差分路由**：UCM 的 Mic 设备写死 `Differential Mux=lin1-rin1`，但本板耳麦麦信号物理在 **lin2-rin2**（我们 `huawei-speaker-mute.sh`/`HEADSET_MIC_ROUTING` 手动设的值）。`Differential Mux=1` 当前值来自脚本/alsactl，**非 UCM** → 这正是当初"手写 amixer"的真正原因（非 UCM 缺失，而是 UCM 上游默认值与本板不符）。
- 耳机**播放**与插拔**自动静音扬声器**(JackHWMute) 已由 UCM 负责，无需手写。扬声器真正静音仍依赖 HWSP0001 I2C 脚本（独立于 UCM）。
- **推荐修正（本次未执行）**：在 `sof-essx8336.conf` BootSequence 追加 `cset "name='Differential Mux' 1"`（纯文本、可逆、不碰内核），即可让耳麦麦由 UCM 托管，摆脱手写 amixer。
- 关联方案 A 纠偏：强制无 DMIC 拓扑须用 **`sof-adl-es8336-ssp0.tplg`**（codec 物理在 ssp0），**不是 ssp1**（ssp1 会路由错 I2S 口→静音）。UCM 的 `Digital Mic Mux 0` 只切 codec 内部路由，**不能**阻止 SOF DMIC 管线被应用打开，故"防 wedge"必须在拓扑层（方案 A），不能靠 UCM。
- 存档文档：`doc/UCM_AUDIT_2026-09-10.zh-CN.md`。

## 部署清单文档（2026-09-10，仅文档、未执行）
- 新增 `doc/DEPLOY_AUDIO_HARDENING.zh-CN.md`：合并"方案 A 强制 ssp0 无 DMIC 拓扑（防 wedge）" + "UCM BootSequence 加 `Differential Mux=1`（耳麦麦免手写 amixer）" 的可部署/可回滚清单。
- 要点已写入文档：必备 ssp0 非 ssp1；tplg_filename 不带 .zst；modprobe.d 方式回滚最干净；tradeoff=丢内置 DMIC 麦；不保证 100%（固件 ABI 不匹配根因未动，对 linux-firmware 升级脆弱）；可选把 .tplg 复制到 /etc 加固。
- 状态：用户已确认并执行 `sudo bash install.sh`（2026-09-10）。`/etc/modprobe.d/sof-es8336-nodmic.conf` 已写入（拓扑 ssp0 无 DMIC，待重启生效）；UCM 因 /usr 只读跳过（运行期脚本兜底）；脚本+服务已装并运行新版（含看门狗）。

## 架构决策：能否并入服务/自动检测（2026-09-10）
- 用户问"并入自动执行和检测脚本？由服务控制？"。结论：**拓扑参数不能由运行期服务控制**。
- 生命周期差异：UCM/Differential Mux 是运行时 mixer 控件，可由服务管（且 `huawei-speaker-mute.sh` 已在做）；强制 ssp0 无 DMIC 拓扑是内核模块参数，仅模块加载时读一次，运行期改它必须重载 snd_sof → 丢设备（用户已验证）。服务在模块加载后才起，碰不到拓扑。
- 正确结构：① 拓扑参数 + UCM 修正放进 `install.sh` 作一次性开机静态配置；② 服务只管插拔监听/功放软静音/Differential Mux 保证；③ "检测"看门狗 = 服务扫 dmesg 找 IPC timeout。
- **看门狗最终定案（2026-09-10 用户修正）：不自动重启，改为系统通知（notify-send）提醒用户手动重启，且仅通知一次（用 /run 哨兵，每次启动只通知一次）。** 必须发到登录用户 DBUS 会话（服务常以 root 跑，需显式指定 DBUS_SESSION_BUS_ADDRESS）。此为方案 A 失败兜底。
- 强调：不要硬让服务动态切拓扑/重载模块（会丢设备）。此决策**已落地代码**（2026-09-10）：`install.sh` 新增 `apply_boot_hardening()`（写 `/etc/modprobe.d/sof-es8336-nodmic.conf` 强制 ssp0 无 DMIC 拓扑 + sed 改 UCM BootSequence 加 `Differential Mux=1`），并在 `install_deps` 后调用；`huawei-speaker-mute.sh` 新增 `notify_user`/`wedge_watchdog`/`wedge_watchdog_loop`（检测 dmesg IPC timeout → notify-send 仅通知一次，不重启），启动后台运行、cleanup 回收 PID。`bash -n` 两脚本均通过。**未实际执行 install.sh（未改动系统）**。SKIP_TOPO/SKIP_UCM 可跳过。
